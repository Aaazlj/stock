version: "3.9"

services:
  quant-fund:
    build: .
    container_name: quant-fund-system
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
